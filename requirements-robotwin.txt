# Simulator dependencies required by the vendored RoboTwin revision.
# Install this in a dedicated environment instead of requirements.txt. The original
# upstream requirements are retained under third_party/RoboTwin/script/ for
# provenance, but they conflict with FastWAM's PyTorch and NumPy pins.
--extra-index-url https://download.pytorch.org/whl/cu128

-r requirements-base.txt

gymnasium==0.29.1
h5py==3.14.0
mplib==0.2.1
numpy==1.26.4
open3d==0.18.0
opencv-python==4.10.0.84
sapien==3.0.0b1
scipy==1.10.1
toppra==0.6.3
transforms3d==0.4.2
trimesh==4.4.3
