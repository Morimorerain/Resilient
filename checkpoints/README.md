# Checkpoints

Downloaded model weights are stored under `checkpoints/fastwam_release/` and ignored by Git. See `manifests/assets.json` and both root README files for pinned download revisions, target paths, sizes, and SHA-256 checksums.
